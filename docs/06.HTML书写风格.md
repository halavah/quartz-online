# 06. HTML 书写风格指南

本指南基于现有的安装指南模板，详细说明了如何编写符合项目规范的HTML文档。

## 📋 文件命名规范

### 目录结构
- 安装指南：`public/chapter01/`
- 技术工具：`public/chapter02/`
- 其他分类：`public/chapter03/` 等

### 文件命名格式
```
[章节编号].[简要标题].html
示例：
- 0101.windows-installation.html
- 0102.macos-installation.html
- 0103.linux-wsl2-installation.html
```

## 🏗️ HTML 基础结构

### 1. 文档声明
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>文章标题</title>
    <meta name="description" content="文章描述">
    <meta name="keywords" content="关键词1,关键词2,关键词3">
</head>
```

### 2. 必需的 meta 标签
- `lang="zh-CN"`：指定中文语言
- `viewport`：响应式设计必需
- `charset="UTF-8"`：字符编码
- `title`：SEO 友好的标题
- `description`：文章摘要
- `keywords`：关键词列表

## 🎨 CSS 样式系统

### 1. CSS 变量定义
```css
:root {
    --primary-color: #0066ff;
    --secondary-color: #00d4ff;
    --accent-color: #ff6b00;
    --dark-bg: #0a0e27;
    --card-bg: #1a1f3a;
    --text-color: #e8eaed;
    --text-secondary: #9aa0a6;
    --border-color: #2a2f4a;
    --gradient-primary: linear-gradient(135deg, #0066ff 0%, #00d4ff 100%);
}
```

### 2. 基础样式
```css
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
    line-height: 1.6;
    color: var(--text-color);
    background: var(--dark-bg);
}
```

## 🧭 响应式设计

### 1. 媒体查询断点
```css
/* 平板设备 */
@media (max-width: 1024px) {
    .main-content {
        padding: 2rem 1.5rem;
        gap: 2rem;
    }
}

/* 手机横屏 */
@media (max-width: 768px) {
    .main-content {
        flex-direction: column;
        padding: 1.5rem;
    }

    .toc-sidebar {
        width: 100%;
        position: static;
    }
}

/* 手机竖屏 */
@media (max-width: 480px) {
    .hero-title {
        font-size: 1.6rem;
    }
}
```

### 2. 网格系统
```css
/* 桌面端：3列 */
.installation-cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
}

/* 平板端：2列 */
@media (max-width: 1024px) {
    .installation-cards {
        grid-template-columns: repeat(2, 1fr);
    }
}

/* 手机端：1列 */
@media (max-width: 768px) {
    .installation-cards {
        grid-template-columns: 1fr;
    }
}
```

## 📑 目录（TOC）系统

### 1. HTML 结构
```html
<div class="toc-sidebar">
    <h2 class="toc-title">📋 目录导航</h2>
    <ul>
        <li><a href="#section1">1. 章节标题</a></li>
        <li><a href="#section2">2. 章节标题</a></li>
    </ul>
</div>
```

### 2. 目录样式
```css
.toc-sidebar {
    position: sticky;
    top: 6rem;
    background: var(--card-bg);
    border: 1px solid var(--border-color);
    border-radius: 0.75rem;
    padding: 1.5rem;
    width: 300px;
    max-height: calc(100vh - 8rem);
    overflow-y: auto;
}

.toc-title {
    font-size: 1.2rem;
    margin-bottom: 1rem;
    color: var(--text-color);
}

.toc-sidebar ul {
    list-style: none;
}

.toc-sidebar a {
    display: block;
    padding: 0.5rem 1rem;
    color: var(--text-secondary);
    text-decoration: none;
    border-radius: 0.25rem;
    transition: all 0.3s ease;
}

.toc-sidebar a:hover,
.toc-sidebar a.active {
    background: var(--primary-color);
    color: white;
}
```

## 📝 内容组织结构

### 1. 英雄区域
```html
<section class="hero-section">
    <div class="hero-container">
        <h1 class="hero-title">主标题</h1>
        <p class="hero-subtitle">副标题/描述</p>
    </div>
</section>
```

### 2. 主要内容区域
```html
<main class="main-content">
    <!-- 侧边栏目录 -->
    <div class="toc-sidebar">...</div>

    <!-- 文章内容 -->
    <div class="content-wrapper">
        <section id="section1">
            <h2>章节标题</h2>
            <p>内容...</p>
        </section>
    </div>
</main>
```

### 3. 章节标题层级
```html
<!-- h1 用于文章主标题（hero区域） -->
<h1>Claude Code Windows 安装指南</h1>

<!-- h2 用于主要章节 -->
<h2>1. 准备工作</h2>

<!-- h3 用于子章节 -->
<h3>1.1 系统要求</h3>

<!-- h4 用于更细分的标题 -->
<h4>1.1.1 硬件要求</h4>
```

## 💻 代码块样式

### 1. 基础代码块
```html
<div class="code-block">
    <pre><code># 代码内容
npm install claude-code</code></pre>
</div>
```

### 2. 带语言标识的代码块
```html
<div class="code-block">
    <div class="code-header">
        <span class="code-language">bash</span>
        <button class="copy-button" onclick="copyCode(this)">复制</button>
    </div>
    <pre><code class="language-bash"># 代码内容
npm install claude-code</code></pre>
</div>
```

### 3. 代码块样式
```css
.code-block {
    background: #1e293b;
    border: 1px solid #334155;
    border-radius: 0.5rem;
    margin: 1.5rem 0;
    overflow: hidden;
}

.code-header {
    background: #334155;
    padding: 0.5rem 1rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.code-header code {
    font-family: 'Fira Code', 'Consolas', monospace;
    color: #cbd5e1;
}

.copy-button {
    background: var(--primary-color);
    color: white;
    border: none;
    padding: 0.25rem 0.75rem;
    border-radius: 0.25rem;
    cursor: pointer;
    transition: background 0.3s ease;
}
```

## 📦 卡片组件

### 1. 安装方法卡片
```html
<div class="installation-cards">
    <div class="install-card">
        <div class="install-icon">📦</div>
        <h3>方法一：npm 安装</h3>
        <p>使用 npm 命令安装 Claude Code</p>
        <div class="code-block">
            <pre><code>npm install -g claude-code</code></pre>
        </div>
    </div>
</div>
```

### 2. 特性卡片
```html
<div class="feature-grid">
    <div class="feature-card">
        <div class="feature-icon">⚡</div>
        <h4>高性能</h4>
        <p>原生性能，无 Node.js 依赖</p>
    </div>
</div>
```

## ✅ 检查清单

在完成 HTML 文档前，请确认以下项目：

### [ ] 基础结构
- [ ] 正确的 DOCTYPE 声明
- [ ] HTML5 语义化标签
- [ ] 正确的 lang 属性
- [ ] 完整的 meta 标签

### [ ] 样式规范
- [ ] 使用项目 CSS 变量
- [ ] 响应式设计适配
- [ ] 深色主题兼容
- [ ] 代码块语法高亮

### [ ] 内容组织
- [ ] 清晰的章节结构
- [ ] 目录导航完整
- [ ] 锚点链接正确
- [ ] 返回顶部按钮

### [ ] 用户体验
- [ ] 合理的字体大小
- [ ] 良好的行高和间距
- [ ] 交互效果（悬停、过渡）
- [ ] 加载性能优化

## 🔧 JavaScript 功能

### 1. 必需的功能
```javascript
// 目录高亮
document.querySelectorAll('.toc-sidebar a').forEach(link => {
    link.addEventListener('click', (e) => {
        e.preventDefault();
        const targetId = link.getAttribute('href').substring(1);
        const targetElement = document.getElementById(targetId);
        if (targetElement) {
            targetElement.scrollIntoView({ behavior: 'smooth' });
        }
    });
});

// 复制代码功能
function copyCode(button) {
    const codeBlock = button.closest('.code-block').querySelector('code');
    const text = codeBlock.textContent;
    navigator.clipboard.writeText(text);
    button.textContent = '已复制！';
    setTimeout(() => {
        button.textContent = '复制';
    }, 2000);
}

// 返回顶部按钮
window.addEventListener('scroll', () => {
    const backToTop = document.getElementById('backToTop');
    if (window.scrollY > 300) {
        backToTop.classList.add('show');
    } else {
        backToTop.classList.remove('show');
    }
});
```

### 2. 返回顶部按钮
```html
<button class="back-to-top" id="backToTop">
    <svg width="24" height="24" viewBox="0 0 24 24">
        <path d="M7 14l5-5 5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
    </svg>
    回到顶部
</button>
```

## 📚 最佳实践

1. **语义化 HTML**：使用正确的 HTML5 语义标签
2. **无障碍访问**：添加必要的 ARIA 标签
3. **性能优化**：压缩图片，使用 WebP 格式
4. **SEO 友好**：正确的标题层级和 meta 标签
5. **代码规范**：缩进一致，注释清晰
6. **测试兼容**：在不同浏览器和设备上测试

## 🚨 常见错误

1. **忘记设置 viewport**：导致移动端显示问题
2. **未使用 CSS 变量**：样式不一致
3. **缺少响应式设计**：移动端体验差
4. **代码块未转义**：导致 HTML 解析错误
5. **锚点链接错误**：目录导航失效

---

**更新时间**：2025-12-13
**项目**：Halavah's Tech
**维护者**：开发团队