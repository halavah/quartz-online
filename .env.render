# Render 部署配置
# 用于部署 Next.js 应用到 Render 平台

# Render 使用 Git 自动部署，无需 API Token
# 但可以设置环境变量用于项目配置

# 构建配置
NODE_ENV=production
BUILD_TARGET=render

# 项目名称
PROJECT_NAME=quartz-online

# Next.js 配置
NEXT_PUBLIC_SITE_URL=https://quartz-online.onrender.com

# Render 特定配置
# RENDER_SERVICE_ID=your_service_id_here (可选)
# RENDER_DEPLOY_HOOK_URL=your_deploy_hook_url_here (可选)
