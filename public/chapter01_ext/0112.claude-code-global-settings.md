---
title: "Claude Code 全局配置与设置"
description: "全面掌握Claude Code配置系统，包含settings.json配置、环境变量、企业部署策略和最佳实践"
publishDate: "2025-12-22"
tags: ["claude-code", "全局配置", "settings.json", "环境变量", "企业部署"]
draft: false
heroImage: "/blog-placeholder-8.jpg"
---

# Claude Code 全局配置与设置

> 全面掌握Claude Code配置系统，包含settings.json配置、环境变量、企业部署策略和最佳实践

## 目录导航

1. [settings.json完全配置](#1-settingsjson完全配置)
2. [环境变量大全](#2-环境变量大全)
3. [企业级部署策略](#3-企业级部署策略)
4. [配置优先级详解](#4-配置优先级详解)
5. [最佳实践建议](#5-最佳实践建议)

## 1. settings.json完全配置

### 1.1 配置文件位置

| 配置类型 | 文件位置 | 描述 |
|----------|----------|------|
| 企业策略 | macOS: `/Library/Application Support/ClaudeCode/policies.json`<br>Linux: `/etc/claude-code/policies.json`<br>Windows: `%PROGRAMDATA%\ClaudeCode\policies.json` | 企业级策略配置，最高优先级 |
| 用户全局 | `~/.claude/settings.json` | 用户个人配置，所有项目生效 |
| 项目共享 | `.claude/settings.json` | 项目共享配置，可提交到Git |
| 项目本地 | `.claude/settings.local.json` | 项目本地配置，被Git忽略 |

### 1.2 完整配置示例

#### 用户全局配置示例

```json
{
  // API和认证配置
  "apiKeyHelper": "/usr/local/bin/company-key-helper.sh",

  // 环境变量
  "env": {
    "ANTHROPIC_BASE_URL": "https://open.bigmodel.cn/api/anthropic",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "glm-4.6",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "glm-4.5-air",
    "NODE_ENV": "development",
    "EDITOR": "code"
  },

  // 权限配置
  "permissions": {
    "allow": [
      "Bash(npm run *)",
      "Bash(git status)",
      "Bash(git diff)",
      "Read(*)",
      "Write(*.md)",
      "Write(*.js)",
      "Write(*.ts)",
      "Write(*.tsx)"
    ],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(sudo *)",
      "Bash(curl *)",
      "Write(~/.ssh/*)",
      "Write(/etc/*)"
    ]
  },

  // 其他配置
  "cleanupPeriodDays": 30,
  "includeCoAuthoredBy": true,

  // 启用的插件
  "enabledPlugins": {
    "frontend-design@claude-code-plugins": true,
    "github@claude-code-plugins": true
  }
}
```

#### 项目级配置示例

```json
{
  // 项目特定环境变量
  "env": {
    "PROJECT_NAME": "my-ecommerce-app",
    "API_BASE_URL": "https://api.myapp.com",
    "DATABASE_URL": "postgresql://localhost:5432/myapp_dev"
  },

  // 项目权限配置
  "permissions": {
    "allow": [
      "Bash(npm run dev)",
      "Bash(npm run test)",
      "Bash(npm run build)",
      "Bash(docker compose up)",
      "Read(src/**/*)",
      "Write(src/**/*)"
    ],
    "deny": [
      "Bash(npm run deploy)",
      "Write(production.env)"
    ]
  },

  // 项目特定配置
  "defaultModel": "claude-sonnet-4",
  "maxOutputTokens": 8192,

  // 团队配置
  "team": {
    "name": "frontend-team",
    "slackChannel": "#frontend-dev",
    "codeReviewAssign": "@senior-dev"
  }
}
```

### 1.3 高级配置选项

#### 性能优化配置

```json
{
  // 性能相关配置
  "env": {
    "BASH_DEFAULT_TIMEOUT_MS": "300000",
    "BASH_MAX_TIMEOUT_MS": "600000",
    "BASH_MAX_OUTPUT_LENGTH": "100000",
    "CLAUDE_CODE_MAX_OUTPUT_TOKENS": "8192",
    "MCP_TIMEOUT": "60000",
    "MCP_TOOL_TIMEOUT": "30000"
  },

  // 模型配置
  "modelSettings": {
    "default": "claude-sonnet-4",
    "reasoning": {
      "maxThinkingTokens": 8192,
      "temperature": 0.1
    },
    "creative": {
      "temperature": 0.8,
      "topP": 0.9
    }
  }
}
```

#### 网络和代理配置

```json
{
  // 代理配置
  "env": {
    "HTTP_PROXY": "http://proxy.company.com:8080",
    "HTTPS_PROXY": "http://proxy.company.com:8080",
    "NO_PROXY": "localhost,127.0.0.1,.company.com",
    "ANTHROPIC_BASE_URL": "https://api-gateway.company.com/anthropic"
  },

  // 网络超时配置
  "network": {
    "timeout": 30000,
    "retries": 3,
    "retryDelay": 1000
  }
}
```

## 2. 环境变量大全

### 2.1 API和认证相关

| 变量名 | 描述 | 示例值 |
|--------|------|--------|
| `ANTHROPIC_API_KEY` | API密钥（X-Api-Key头） | `sk-ant-...` |
| `ANTHROPIC_AUTH_TOKEN` | 认证令牌（Authorization头） | `sk-...` |
| `ANTHROPIC_BASE_URL` | 自定义API端点 | `https://api.anthropic.com` |
| `ANTHROPIC_CUSTOM_HEADERS` | 自定义HTTP头 | `X-Custom-Header: value` |

#### 配置示例

```bash
# 基础认证配置
export ANTHROPIC_AUTH_TOKEN="sk-ant-api03-..."
export ANTHROPIC_BASE_URL="https://open.bigmodel.cn/api/anthropic"

# 自定义请求头
export ANTHROPIC_CUSTOM_HEADERS="X-Team: frontend,X-Project: ecommerce"

# API密钥助手
export CLAUDE_CODE_API_KEY_HELPER_TTL_MS="3600000"
```

### 2.2 模型配置

| 变量名 | 描述 | 示例值 |
|--------|------|--------|
| `ANTHROPIC_MODEL` | 默认模型名称 | `claude-sonnet-4` |
| `ANTHROPIC_DEFAULT_SONNET_MODEL` | Sonnet模型映射 | `glm-4.6` |
| `ANTHROPIC_DEFAULT_HAIKU_MODEL` | Haiku模型映射 | `glm-4.5-air` |
| `ANTHROPIC_DEFAULT_OPUS_MODEL` | Opus模型映射 | `glm-4.6` |
| `ANTHROPIC_SMALL_FAST_MODEL` | 快速小模型 | `glm-4.5-air` |
| `MAX_THINKING_TOKENS` | 思考Token预算 | `8192` |

#### 配置示例

```bash
# GLM模型映射
export ANTHROPIC_DEFAULT_SONNET_MODEL="glm-4.6"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="glm-4.5-air"
export ANTHROPIC_DEFAULT_OPUS_MODEL="glm-4.6"

# 思考配置
export MAX_THINKING_TOKENS="16384"
export ANTHROPIC_MODEL="GLM-4.6"
```

### 2.3 Bash和执行环境

| 变量名 | 描述 | 默认值 |
|--------|------|--------|
| `BASH_DEFAULT_TIMEOUT_MS` | Bash命令默认超时 | `120000` |
| `BASH_MAX_TIMEOUT_MS` | Bash命令最大超时 | `600000` |
| `BASH_MAX_OUTPUT_LENGTH` | Bash输出最大长度 | `30000` |
| `CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR` | 保持项目工作目录 | `false` |

#### 配置示例

```bash
# Bash超时配置
export BASH_DEFAULT_TIMEOUT_MS="300000"
export BASH_MAX_TIMEOUT_MS="1800000"
export BASH_MAX_OUTPUT_LENGTH="100000"

# 工作目录配置
export CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR="true"
```

### 2.4 功能开关

| 变量名 | 描述 | 值 |
|--------|------|----|
| `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC` | 禁用非必要流量 | `1` |
| `DISABLE_AUTOUPDATER` | 禁用自动更新 | `1` |
| `DISABLE_BUG_COMMAND` | 禁用/bug命令 | `1` |
| `DISABLE_COST_WARNINGS` | 禁用成本警告 | `1` |
| `DISABLE_ERROR_REPORTING` | 禁用错误报告 | `1` |
| `DISABLE_TELEMETRY` | 禁用遥测 | `1` |
| `DISABLE_NON_ESSENTIAL_MODEL_CALLS` | 禁用非必要模型调用 | `1` |

#### 配置示例

```bash
# 禁用非必要功能
export CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC="1"
export DISABLE_AUTOUPDATER="1"
export DISABLE_TELEMETRY="1"
export DISABLE_ERROR_REPORTING="1"

# 保留必要功能
export DISABLE_COST_WARNINGS="0"  # 保留成本提醒
```

### 2.5 网络和代理

| 变量名 | 描述 | 示例值 |
|--------|------|--------|
| `HTTP_PROXY` | HTTP代理服务器 | `http://proxy:8080` |
| `HTTPS_PROXY` | HTTPS代理服务器 | `https://proxy:8080` |
| `NO_PROXY` | 代理绕过列表 | `localhost,127.0.0.1` |
| `CLAUDE_CODE_SKIP_BEDROCK_AUTH` | 跳过Bedrock认证 | `1` |
| `CLAUDE_CODE_SKIP_VERTEX_AUTH` | 跳过Vertex认证 | `1` |

#### 配置示例

```bash
# 代理配置
export HTTP_PROXY="http://proxy.company.com:8080"
export HTTPS_PROXY="http://proxy.company.com:8080"
export NO_PROXY="localhost,127.0.0.1,.company.com,.local"

# 特殊平台配置
export CLAUDE_CODE_SKIP_BEDROCK_AUTH="1"
export CLAUDE_CODE_USE_VERTEX="1"
```

## 3. 企业级部署策略

### 3.1 企业策略配置

#### macOS企业配置

```bash
# 创建企业策略目录
sudo mkdir -p "/Library/Application Support/ClaudeCode"

# 创建策略文件
sudo tee "/Library/Application Support/ClaudeCode/policies.json" << 'EOF'
{
  "permissions": {
    "allow": [
      "Read(/Users/*/projects/*)",
      "Write(/Users/*/projects/*/src/**/*)",
      "Bash(npm run test)",
      "Bash(npm run build)",
      "Bash(git status)",
      "Bash(git diff)"
    ],
    "deny": [
      "Write(/etc/*)",
      "Write(~/.ssh/*)",
      "Bash(sudo *)",
      "Bash(curl *)",
      "Bash(wget *)",
      "Bash(rm -rf /)"
    ]
  },

  "env": {
    "ANTHROPIC_BASE_URL": "https://api-gateway.company.com/anthropic",
    "ANTHROPIC_API_KEY_HELPER": "/usr/local/bin/company-key-helper.sh",
    "CLAUDE_CODE_ENABLE_TELEMETRY": "0",
    "DISABLE_TELEMETRY": "1",
    "DISABLE_ERROR_REPORTING": "1",
    "COMPANY_POLICY": "strict"
  },

  "cleanupPeriodDays": 7,
  "includeCoAuthoredBy": true,

  "network": {
    "allowedHosts": [
      "api-gateway.company.com",
      "github.com",
      "npm.pkg.github.com"
    ],
    "blockedHosts": [
      "pastebin.com",
      "transfer.sh"
    ]
  },

  "audit": {
    "enabled": true,
    "logPath": "/var/log/claude-code/audit.log",
    "logLevel": "info"
  }
}
EOF

# 设置权限
sudo chown -R root:wheel "/Library/Application Support/ClaudeCode"
sudo chmod 755 "/Library/Application Support/ClaudeCode"
sudo chmod 644 "/Library/Application Support/ClaudeCode/policies.json"
```

#### Linux企业配置

```bash
# 创建企业配置目录
sudo mkdir -p /etc/claude-code

# 创建策略文件
sudo tee /etc/claude-code/policies.json << 'EOF'
{
  "permissions": {
    "allow": [
      "Read(/home/*/projects/*)",
      "Write(/home/*/projects/*/src/**/*)",
      "Bash(npm test)",
      "Bash(make test)",
      "Bash(git status)"
    ]
  },

  "env": {
    "ANTHROPIC_BASE_URL": "https://api.company.com",
    "CLAUDE_CODE_ENABLE_TELEMETRY": "0",
    "HTTP_PROXY": "http://proxy.company.com:8080",
    "HTTPS_PROXY": "http://proxy.company.com:8080"
  },

  "security": {
    "requireKeyAuth": true,
    "maxSessionDuration": 28800,
    "idleTimeout": 1800
  }
}
EOF

# 设置权限
sudo chown -R root:root /etc/claude-code
sudo chmod 755 /etc/claude-code
sudo chmod 644 /etc/claude-code/policies.json
```

### 3.2 API密钥管理

#### 密钥助手脚本

```bash
#!/bin/bash
# /usr/local/bin/company-key-helper.sh

# 从公司密钥管理系统获取临时密钥
RESPONSE=$(curl -s -X POST \
  -H "Authorization: Bearer $COMP_SERVICE_TOKEN" \
  -H "Content-Type: application/json" \
  https://keymanager.company.com/api/keys/claude \
  -d '{
    "ttl": 3600,
    "scopes": ["read", "write"]
  }')

# 解析响应并返回密钥
echo $RESPONSE | jq -r '.apiKey // empty'

# 错误处理
if [ $? -ne 0 ]; then
  echo "Error: Failed to obtain API key" >&2
  exit 1
fi
```

#### 企业密钥轮换

```bash
#!/bin/bash
# /usr/local/bin/rotate-claude-keys.sh

LOG_FILE="/var/log/claude-key-rotation.log"

log() {
  echo "[$(date '+%Y-%m-%d %H:%M:%S')] $1" >> $LOG_FILE
}

# 获取当前密钥信息
CURRENT_KEY_INFO=$(curl -s -H "Authorization: Bearer $ADMIN_TOKEN" \
  https://keymanager.company.com/api/keys/current)

log "Starting key rotation..."

# 生成新密钥
NEW_KEY_RESPONSE=$(curl -s -X POST \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  https://keymanager.company.com/api/keys \
  -d '{"ttl": 86400, "scopes": ["read", "write"]}')

NEW_KEY=$(echo $NEW_KEY_RESPONSE | jq -r '.apiKey')

# 更新配置
sudo sed -i "s/ANTHROPIC_AUTH_TOKEN=.*/ANTHROPIC_AUTH_TOKEN=\"$NEW_KEY\"/" \
  /etc/environment

log "Key rotation completed successfully"

# 通知管理员
curl -s -X POST \
  -H "Content-Type: application/json" \
  https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK \
  -d "{\"text\":\"Claude Code API key rotated successfully\"}"
```

### 3.3 监控和审计

#### 审计日志配置

```json
{
  "audit": {
    "enabled": true,
    "logPath": "/var/log/claude-code/audit.log",
    "logLevel": "info",
    "logFormat": "json",
    "maxFileSize": "100MB",
    "maxFiles": 10,
    "events": [
      "session_start",
      "session_end",
      "command_execution",
      "file_access",
      "permission_change",
      "error_occurred"
    ]
  }
}
```

#### 监控脚本

```bash
#!/bin/bash
# /usr/local/bin/monitor-claude-usage.sh

ALERT_THRESHOLD=1000  # 每日API调用阈值
LOG_FILE="/var/log/claude-usage.log"
WEBHOOK_URL="https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK"

# 统计今日使用量
TODAY_USAGE=$(grep $(date +%Y-%m-%d) /var/log/claude-code/audit.log | wc -l)

if [ $TODAY_USAGE -gt $ALERT_THRESHOLD ]; then
  MESSAGE="⚠️ Claude Code usage alert: $TODAY_USAGE API calls today (threshold: $ALERT_THRESHOLD)"

  # 发送告警
  curl -X POST -H 'Content-type: application/json' \
    --data "{\"text\":\"$MESSAGE\"}" \
    $WEBHOOK_URL

  echo "[$(date)] Usage alert sent: $TODAY_USAGE calls" >> $LOG_FILE
fi

# 生成使用报告
cat > /var/www/reports/claude-usage-$(date +%Y%m%d).json << EOF
{
  "date": "$(date +%Y-%m-%d)",
  "totalCalls": $TODAY_USAGE,
  "users": $(grep $(date +%Y-%m-%d) /var/log/claude-code/audit.log | jq -s '. | group_by(.user) | map({user: .[0].user, count: length})'),
  "topCommands": $(grep $(date +%Y-%m-%d) /var/log/claude-code/audit.log | jq -s '. | group_by(.command) | map({command: .[0].command, count: length}) | sort_by(.count) | reverse'),
  "errors": $(grep $(date +%Y-%m-%d) /var/log/claude-code/audit.log | jq 'select(.type == "error") | length')
}
EOF
```

## 4. 配置优先级详解

### 4.1 优先级层次

```
1. 命令行参数          (最高优先级)
2. 环境变量
3. 项目本地配置        (.claude/settings.local.json)
4. 项目共享配置        (.claude/settings.json)
5. 用户全局配置        (~/.claude/settings.json)
6. 企业策略配置        (最低优先级)
```

### 4.2 配置继承示例

#### 场景：多层配置继承

```json
// 企业策略 - 最低优先级
{
  "permissions": {
    "allow": ["Read(*)"],
    "deny": ["Bash(sudo*)", "Write(/etc/*)"]
  },
  "env": {
    "ANTHROPIC_BASE_URL": "https://api.company.com",
    "DISABLE_TELEMETRY": "1"
  }
}
```

```json
// 用户全局配置
{
  "permissions": {
    "allow": ["Write(*.md)", "Write(*.txt)"],  // 追加权限
    "deny": ["Bash(rm*)"]  // 追加禁止
  },
  "env": {
    "EDITOR": "code",  // 新增环境变量
    "ANTHROPIC_BASE_URL": "https://api.personal.com"  // 覆盖企业配置
  }
}
```

```json
// 项目共享配置
{
  "permissions": {
    "allow": ["Bash(npm run*)"],  // 项目特定权限
    "deny": ["Write(production.env)"]  // 项目特定禁止
  },
  "env": {
    "NODE_ENV": "development",  // 项目环境
    "PROJECT_NAME": "my-app"
  }
}
```

```json
// 项目本地配置 - 最高优先级
{
  "environmentVariables": [
    {
      "name": "ANTHROPIC_AUTH_TOKEN",
      "value": "personal-api-key"  // 个人密钥
    }
  ],
  "permissions": {
    "allow": ["Bash(npm run deploy)"]  // 覆盖项目禁止
  }
}
```

#### 最终生效配置

```json
{
  "permissions": {
    "allow": [
      "Read(*)",                    // 来自企业
      "Write(*.md)",                // 来自用户
      "Write(*.txt)",               // 来自用户
      "Bash(npm run*)",             // 来自项目
      "Bash(npm run deploy)"        // 来自本地（覆盖项目禁止）
    ],
    "deny": [
      "Bash(sudo*)",                // 来自企业
      "Write(/etc/*)",              // 来自企业
      "Bash(rm*)",                  // 来自用户
      "Write(production.env)"       // 来自项目
    ]
  },
  "env": {
    "ANTHROPIC_BASE_URL": "https://api.personal.com",  // 用户配置覆盖企业
    "DISABLE_TELEMETRY": "1",                         // 来自企业
    "EDITOR": "code",                                 // 来自用户
    "NODE_ENV": "development",                        // 来自项目
    "PROJECT_NAME": "my-app"                          // 来自项目
  },
  "environmentVariables": [
    {
      "name": "ANTHROPIC_AUTH_TOKEN",
      "value": "personal-api-key"                     // 来自本地配置
    }
  ]
}
```

### 4.3 动态优先级调整

#### 运行时配置覆盖

```bash
# 命令行参数最高优先级
claude --model claude-opus-4 \
       --allowedTools "Bash(npm*)" "Read(*)" \
       --disallowedTools "Write(*)" \
       "分析项目结构"

# 临时环境变量覆盖
ANTHROPIC_MODEL="claude-opus-4" \
claude "执行复杂任务"
```

#### 配置验证命令

```bash
# 查看最终生效的配置
claude /config

# 查看特定配置项
claude /config permissions

# 查看环境变量
claude /config env
```

## 5. 最佳实践建议

### 5.1 安全配置

#### 敏感信息保护

```json
{
  // ❌ 错误：硬编码密钥
  "env": {
    "ANTHROPIC_AUTH_TOKEN": "sk-ant-api03-..."
  }
}

// ✅ 正确：使用密钥助手
{
  "apiKeyHelper": "/usr/local/bin/secure-key-helper.sh"
}

// ✅ 正确：使用本地配置文件
// .claude/settings.local.json (被git忽略)
{
  "environmentVariables": [
    {
      "name": "ANTHROPIC_AUTH_TOKEN",
      "value": "sk-ant-api03-..."
    }
  ]
}
```

#### 权限最小化原则

```json
{
  // ❌ 过于宽松的权限
  "permissions": {
    "allow": ["Bash(*)", "Write(*)"]
  }
}

// ✅ 精确的权限控制
{
  "permissions": {
    "allow": [
      "Bash(npm run test)",
      "Bash(npm run build)",
      "Bash(git status)",
      "Read(src/**/*)",
      "Write(src/**/*.ts)",
      "Write(src/**/*.js)"
    ]
  }
}
```

### 5.2 性能优化

#### Token使用优化

```json
{
  "env": {
    // 限制输出长度减少Token使用
    "CLAUDE_CODE_MAX_OUTPUT_TOKENS": "4096",

    // 短任务使用小模型
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "glm-4.5-air",

    // 启用会话压缩
    "CLAUDE_AUTO_COMPRESS_THRESHOLD": "50000"
  }
}
```

#### 网络优化

```json
{
  "env": {
    // 设置合理的超时时间
    "BASH_DEFAULT_TIMEOUT_MS": "120000",

    // 使用本地镜像加速
    "NPM_REGISTRY": "https://registry.npmmirror.com",

    // 配置代理
    "HTTP_PROXY": "http://proxy:8080"
  }
}
```

### 5.3 团队协作

#### 标准化项目配置

```json
// .claude/settings.json (提交到git)
{
  "team": {
    "name": "frontend-team",
    "version": "1.0.0",
    "lastUpdated": "2025-12-22"
  },

  "permissions": {
    "allow": [
      "Bash(npm run lint)",
      "Bash(npm run test)",
      "Bash(npm run build)"
    ]
  },

  "env": {
    "NODE_ENV": "development",
    "TS_NODE_PROJECT": "tsconfig.json"
  },

  "scripts": {
    "setup": "npm install && npm run build",
    "test": "npm run test && npm run lint",
    "deploy": "npm run build && npm run deploy:staging"
  }
}
```

#### 个人配置模板

```json
// ~/.claude/settings.json.template
{
  "personal": {
    "name": "{{USER_NAME}}",
    "email": "{{USER_EMAIL}}"
  },

  "env": {
    "EDITOR": "code",
    "GIT_AUTHOR_NAME": "{{USER_NAME}}",
    "GIT_AUTHOR_EMAIL": "{{USER_EMAIL}}"
  }
}
```

### 5.4 维护和更新

#### 配置版本控制

```bash
# 创建配置备份
cp ~/.claude/settings.json ~/.claude/settings.json.backup.$(date +%Y%m%d)

# 配置变更记录
echo "$(date): Updated Claude Code configuration" >> ~/.claude/changelog.md
```

#### 定期维护脚本

```bash
#!/bin/bash
# claude-maintenance.sh

echo "开始Claude Code维护..."

# 清理缓存
find ~/.claude/cache -name "*.tmp" -mtime +7 -delete

# 更新配置模板
curl -s https://raw.githubusercontent.com/company/claude-config/main/settings.json.template \
  -o ~/.claude/settings.json.template

# 验证配置
claude /doctor

# 生成使用报告
claude -p "生成本月使用报告" > ~/claude-report-$(date +%Y%m).md

echo "维护完成"
```

## 下一步

配置完成后，建议继续学习：

- [Claude Code高级自动化技巧](./0113.claude-code-automation.md)
- [Claude Code故障排除指南](./0114.claude-code-troubleshooting.md)

## 参考资源

- [Claude Code配置文档](https://docs.claude.com/en/docs/claude-code/configuration)
- [企业部署指南](https://docs.claude.com/en/docs/claude-code/enterprise)
- [安全最佳实践](https://docs.claude.com/en/docs/claude-code/security)

> 💡 **提示**: 使用 `claude /config` 命令可以查看当前生效的完整配置。定期运行 `claude /doctor` 检查配置健康状态，确保系统正常运行。