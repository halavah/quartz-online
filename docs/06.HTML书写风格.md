# AI HTML 生成硬性提示词规范

> **目标**: 让大模型快速理解项目 HTML 书写风格,生成统一、规范、高质量的技术文档 HTML 页面

---

## 🎯 核心要求 (CRITICAL REQUIREMENTS)

当你被要求生成或修改 HTML 文档时,**必须严格遵循**以下规范:

### 1. 文档基础结构 (Document Structure)

**必需的 HTML 结构:**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>[项目名称] - [简短描述]</title>
    <meta name="description" content="[详细描述,80-160字符]">
    <meta name="keywords" content="[关键词1],[关键词2],[关键词3],[...]">
</head>
<body>
    <!-- Hero Section -->
    <section class="hero-section">
        <div class="hero-container">
            <span class="hero-badge">[项目类型标签]</span>
            <h1 class="hero-title">[项目标题]</h1>
            <p class="hero-subtitle">[项目副标题/描述]</p>
            <div class="hero-meta">
                <span class="meta-item"><strong>许可证:</strong> [License]</span>
                <span class="meta-item"><strong>语言:</strong> [Language]</span>
                <span class="meta-item"><strong>平台:</strong> [Platform]</span>
            </div>
        </div>
    </section>

    <!-- Main Content Area -->
    <main class="main-content">
        <!-- TOC Sidebar -->
        <nav class="toc-sidebar">
            <h2 class="toc-title">📋 目录导航</h2>
            <ul>
                <li><a href="#section1">1. 章节标题</a></li>
                <li><a href="#section2">2. 章节标题</a></li>
                <!-- 更多���录项 -->
            </ul>
        </nav>

        <!-- Content Wrapper -->
        <div class="content-wrapper">
            <section id="section1" class="section">
                <h2 class="section-title">章节标题</h2>
                <p class="section-description">章节简短描述</p>
                <!-- 内容 -->
            </section>
        </div>
    </main>

    <!-- Back to Top Button -->
    <button class="back-to-top" id="backToTop">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
            <path d="M7 14l5-5 5 5" stroke="currentColor" stroke-width="2"
                  stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        回到顶部
    </button>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-container">
            <p id="copyright-text">© 2025 Halavah's Tech. All rights reserved.</p>
            <p id="beian-number"></p>
        </div>
    </footer>

    <script>
        // JavaScript 代码 (见下方)
    </script>
</body>
</html>
```

---

## 🎨 CSS 样式系统 (Mandatory CSS)

### 必需的 CSS 变量

**在 `<style>` 标签中,必须首先定义以下 CSS 变量:**

```css
:root {
    /* 主色调 */
    --primary-color: #0066ff;
    --secondary-color: #00d4ff;
    --accent-color: #ff6b00;

    /* 背景色 */
    --dark-bg: #0a0e27;
    --card-bg: #1a1f3a;

    /* 文字色 */
    --text-color: #e8eaed;
    --text-secondary: #9aa0a6;

    /* 边框和辅助色 */
    --border-color: #2a2f4a;
    --success-color: #00c853;
    --warning-color: #ff9800;
    --error-color: #f44336;

    /* 渐变 */
    --gradient-primary: linear-gradient(135deg, #0066ff 0%, #00d4ff 100%);
    --gradient-hero: linear-gradient(135deg, #0a0e27 0%, #1a1f3a 50%, #0a0e27 100%);
    --shadow-glow: 0 0 30px rgba(0, 102, 255, 0.3);
}
```

### 必需的基础样式

```css
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
    scroll-padding-top: 100px;
}

body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto',
                 'Helvetica Neue', Arial, sans-serif;
    line-height: 1.6;
    color: var(--text-color);
    background: var(--dark-bg);
    overflow-x: hidden;
}
```

### Hero Section 样式模板

```css
.hero-section {
    background: var(--gradient-hero);
    padding: 4rem 2rem 3rem;
    position: relative;
    overflow: hidden;
}

.hero-section::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    background:
        radial-gradient(circle at 20% 50%, rgba(0, 102, 255, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 80% 50%, rgba(0, 212, 255, 0.1) 0%, transparent 50%);
    pointer-events: none;
}

.hero-container {
    max-width: 1200px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
}

.hero-badge {
    display: inline-block;
    background: rgba(0, 102, 255, 0.2);
    color: var(--secondary-color);
    padding: 0.5rem 1rem;
    border-radius: 50px;
    font-size: 0.875rem;
    font-weight: 600;
    border: 1px solid rgba(0, 212, 255, 0.3);
    margin-bottom: 1rem;
}

.hero-title {
    font-size: 2.5rem;
    font-weight: 800;
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 1rem;
    line-height: 1.2;
}

.hero-subtitle {
    font-size: 1.25rem;
    color: var(--text-secondary);
    margin-bottom: 1.5rem;
}

.hero-meta {
    display: flex;
    gap: 2rem;
    flex-wrap: wrap;
    margin-top: 1.5rem;
}

.meta-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--text-secondary);
    font-size: 0.9rem;
}

.meta-item strong {
    color: var(--text-color);
}
```

### Main Content 布局样式

```css
.main-content {
    max-width: 1400px;
    margin: 0 auto;
    padding: 3rem 2rem;
    display: flex;
    gap: 3rem;
    align-items: flex-start;
}

.content-wrapper {
    flex: 1;
    min-width: 0;
}

.section {
    margin-bottom: 3rem;
}

.section-title {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid var(--border-color);
}

.section-description {
    color: var(--text-secondary);
    font-size: 1.1rem;
    margin-bottom: 1.5rem;
    line-height: 1.8;
}
```

### TOC Sidebar 样式 (固定右侧)

```css
.toc-sidebar {
    width: 280px;
    position: sticky;
    top: 6rem;
    background: var(--card-bg);
    border: 1px solid var(--border-color);
    border-radius: 12px;
    padding: 1.5rem;
    max-height: calc(100vh - 4rem);
    overflow-y: auto;
    order: 2; /* 确保在右侧 */
}

.toc-title {
    font-size: 1.2rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: var(--text-color);
}

.toc-sidebar ul {
    list-style: none;
}

.toc-sidebar li {
    margin-bottom: 0.5rem;
}

.toc-sidebar a {
    display: block;
    padding: 0.5rem 1rem;
    color: var(--text-secondary);
    text-decoration: none;
    border-radius: 6px;
    transition: all 0.3s ease;
    font-size: 0.9rem;
}

.toc-sidebar a:hover {
    background: rgba(0, 102, 255, 0.1);
    color: var(--primary-color);
    transform: translateX(4px);
}

.toc-sidebar a.active {
    background: var(--primary-color);
    color: white;
    font-weight: 600;
}

/* TOC Sidebar Scrollbar */
.toc-sidebar::-webkit-scrollbar {
    width: 6px;
}

.toc-sidebar::-webkit-scrollbar-track {
    background: rgba(255, 255, 255, 0.05);
    border-radius: 3px;
}

.toc-sidebar::-webkit-scrollbar-thumb {
    background: var(--primary-color);
    border-radius: 3px;
}
```

### 代码块样式

```css
.code-block {
    background: #1e293b;
    border: 1px solid #334155;
    border-radius: 8px;
    margin: 1.5rem 0;
    overflow: hidden;
}

.code-header {
    background: #334155;
    padding: 0.75rem 1rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid #475569;
}

.code-title {
    color: var(--secondary-color);
    font-size: 0.875rem;
    font-weight: 600;
}

.copy-btn {
    background: var(--primary-color);
    color: white;
    border: none;
    padding: 0.375rem 0.875rem;
    border-radius: 6px;
    font-size: 0.875rem;
    cursor: pointer;
    transition: all 0.3s ease;
}

.copy-btn:hover {
    background: var(--secondary-color);
    transform: translateY(-2px);
}

.code-block pre {
    padding: 1.25rem;
    overflow-x: auto;
    margin: 0;
}

.code-block code {
    font-family: 'Fira Code', 'Consolas', 'Monaco', monospace;
    font-size: 0.9rem;
    line-height: 1.6;
    color: #cbd5e1;
}

/* 代码语法高亮 */
.comment { color: #6b7280; font-style: italic; }
.keyword { color: #60a5fa; font-weight: 600; }
.string { color: #34d399; }
.number { color: #f59e0b; }
.function { color: #a78bfa; }
```

### 卡片样式

```css
.card {
    background: var(--card-bg);
    border: 1px solid var(--border-color);
    border-radius: 12px;
    padding: 2rem;
    margin-bottom: 1.5rem;
    transition: all 0.3s ease;
}

.card:hover {
    border-color: var(--primary-color);
    box-shadow: var(--shadow-glow);
    transform: translateY(-4px);
}

.card h3 {
    color: var(--text-color);
    font-size: 1.5rem;
    margin-bottom: 1rem;
}

.card p {
    color: var(--text-secondary);
    line-height: 1.8;
    margin-bottom: 1rem;
}
```

### Feature Grid 样式

```css
.feature-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    margin: 2rem 0;
}

.feature-item {
    background: var(--card-bg);
    border: 1px solid var(--border-color);
    border-radius: 12px;
    padding: 2rem;
    text-align: center;
    transition: all 0.3s ease;
}

.feature-item:hover {
    border-color: var(--primary-color);
    transform: translateY(-8px);
    box-shadow: var(--shadow-glow);
}

.feature-item .icon {
    font-size: 3rem;
    margin-bottom: 1rem;
}

.feature-item h4 {
    font-size: 1.2rem;
    color: var(--text-color);
    margin-bottom: 0.75rem;
}

.feature-item p {
    color: var(--text-secondary);
    font-size: 0.95rem;
    line-height: 1.6;
}
```

### Info Box 样式

```css
.info-box {
    background: rgba(0, 102, 255, 0.1);
    border-left: 4px solid var(--primary-color);
    padding: 1rem 1.5rem;
    border-radius: 6px;
    margin: 1.5rem 0;
}

.info-box.success {
    background: rgba(0, 200, 83, 0.1);
    border-left-color: var(--success-color);
}

.info-box.warning {
    background: rgba(255, 152, 0, 0.1);
    border-left-color: var(--warning-color);
}

.info-box strong {
    color: var(--text-color);
}

.info-box code {
    background: rgba(0, 0, 0, 0.3);
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    font-family: 'Fira Code', monospace;
    font-size: 0.9rem;
}
```

### Back to Top Button 样式

```css
.back-to-top {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    width: 50px;
    height: 50px;
    background: var(--primary-color);
    color: white;
    border: none;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    font-size: 0.7rem;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
    z-index: 1000;
    box-shadow: 0 4px 12px rgba(0, 102, 255, 0.4);
}

.back-to-top.show {
    opacity: 1;
    visibility: visible;
}

.back-to-top:hover {
    background: var(--secondary-color);
    transform: translateY(-5px);
    box-shadow: 0 6px 20px rgba(0, 212, 255, 0.5);
}

.back-to-top svg {
    margin-bottom: 2px;
}
```

### Footer 样式

```css
.footer {
    background: var(--card-bg);
    border-top: 1px solid var(--border-color);
    padding: 2rem;
    text-align: center;
    margin-top: 4rem;
}

.footer-container {
    max-width: 1200px;
    margin: 0 auto;
    color: var(--text-secondary);
    font-size: 0.9rem;
}

.footer-container p {
    margin: 0.5rem 0;
}
```

---

## 📱 响应式设计 (Responsive Design)

**必须包含以下媒体查询:**

```css
/* TOC Sidebar 基础响应式 (移动端优先) */
@media (max-width: 1024px) {
    .toc-sidebar {
        position: static;
        width: 100%;
        max-height: none;
        order: -1; /* Show TOC before content on mobile */
    }
}

/* Back to top button 基础样式调整 */
.back-to-top {
    position: fixed;
    bottom: 30px;
    right: 30px;
    background: var(--primary-color);
    color: white;
    border: none;
    padding: 12px 20px;
    border-radius: 30px;
    cursor: pointer;
    display: none;
    align-items: center;
    gap: 8px;
    z-index: 1000;
    transition: all 0.3s ease;
    font-size: 14px;
    font-weight: 500;
    box-shadow: 0 4px 12px rgba(0, 102, 255, 0.3);
}

.back-to-top:hover {
    background: var(--secondary-color);
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0, 212, 255, 0.4);
}

.back-to-top.show {
    display: flex;
}

.back-to-top svg {
    width: 16px;
    height: 16px;
}

/* 小屏设备 back-to-top 调整 */
@media (max-width: 768px) {
    .hero-title {
        font-size: 2rem;
    }

    .section-title {
        font-size: 1.5rem;
    }

    .main-content {
        padding: 2rem 1rem;
    }

    .back-to-top {
        bottom: 20px;
        right: 20px;
        padding: 10px 16px;
        font-size: 13px;
    }
}

/* 平板设备 (≤1024px) - 详细响应式规则 */
@media (max-width: 1024px) {
    .main-content {
        padding: 2rem 1.5rem;
        gap: 2rem;
    }

    .hero-section {
        padding: 3rem 1.5rem 2rem;
    }

    .hero-title {
        font-size: 2.2rem;
    }

    .toc-sidebar {
        width: 280px;
    }

    /* 根据实际页面调整网格列数 */
    .prerequisites-grid,
    .feature-grid {
        grid-template-columns: repeat(2, 1fr);
    }
}

/* 手机横屏/平板竖屏 (≤768px) */
@media (max-width: 768px) {
    .main-content {
        flex-direction: column;
        padding: 1.5rem;
    }

    .hero-section {
        padding: 2rem 1rem;
    }

    .hero-title {
        font-size: 1.8rem;
    }

    .hero-subtitle {
        font-size: 1.1rem;
    }

    .toc-sidebar {
        width: 100%;
        margin-bottom: 2rem;
        position: static;
        border-radius: 0.75rem;
    }

    .toc-sidebar ul {
        columns: 1;
    }

    /* 所有网格布局改为单列 */
    .install-methods,
    .installation-cards,
    .prerequisites-grid,
    .feature-grid {
        grid-template-columns: 1fr;
    }

    /* 代码块响应式调整 */
    .code-block {
        padding: 1rem;
        font-size: 0.9rem;
    }

    .code-block code {
        white-space: pre-wrap;
        word-break: break-all;
    }

    .config-form {
        padding: 1.5rem;
    }
}

/* 手机竖屏 (≤480px) - 最小屏幕优化 */
@media (max-width: 480px) {
    .hero-title {
        font-size: 1.6rem;
    }

    .hero-subtitle {
        font-size: 1rem;
    }

    h2 {
        font-size: 1.5rem;
    }

    h3 {
        font-size: 1.3rem;
    }

    .card,
    .install-card,
    .feature-card {
        padding: 1.5rem;
    }

    .config-form {
        padding: 1rem;
    }

    .feature-item {
        padding: 1rem;
    }

    .feature-list li {
        padding: 0.75rem;
    }

    .code-block {
        padding: 0.75rem;
    }

    /* 统计网格在最小屏幕上单列显示 */
    .stat-grid {
        grid-template-columns: 1fr;
    }
}
```

**重要提示**:
- TOC Sidebar 在移动端 (≤1024px) 必须切换为静态定位,显示在内容上方
- 所有网格布局在 768px 以下必须改为单列
- 代码块在移动端需要启用文本换行 (`white-space: pre-wrap`)
- 所有元素的 padding 在小屏幕上应适当减小

---

## ⚙️ JavaScript 功能 (Required JavaScript)

**必须在 `</body>` 前包含以下 JavaScript 代码:**

```javascript
<script>
    // 1. 加载配置文件 (网站备案号等)
    fetch('/config.json')
        .then(response => response.json())
        .then(data => {
            const { beianNumber, copyrightText } = data.config;
            document.getElementById('copyright-text').textContent =
                copyrightText || '© 2025 Halavah\'s Tech. All rights reserved.';
            if (beianNumber) {
                document.getElementById('beian-number').textContent = beianNumber;
            }
        })
        .catch(error => console.error('Failed to load config:', error));

    // 2. TOC 点击平滑滚动
    document.querySelectorAll('.toc-sidebar a').forEach(link => {
        link.addEventListener('click', (e) => {
            e.preventDefault();
            const targetId = link.getAttribute('href').substring(1);
            const targetElement = document.getElementById(targetId);
            if (targetElement) {
                targetElement.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            }
        });
    });

    // 3. TOC 激活状态跟踪 (Intersection Observer)
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                document.querySelectorAll('.toc-sidebar a').forEach(link => {
                    link.classList.remove('active');
                    if (link.getAttribute('href') === `#${entry.target.id}`) {
                        link.classList.add('active');
                    }
                });
            }
        });
    }, {
        rootMargin: '-100px 0px -80% 0px'
    });

    document.querySelectorAll('section[id]').forEach(section => {
        observer.observe(section);
    });

    // 4. 回到顶部按钮
    const backToTop = document.getElementById('backToTop');

    window.addEventListener('scroll', () => {
        if (window.scrollY > 300) {
            backToTop.classList.add('show');
        } else {
            backToTop.classList.remove('show');
        }
    });

    backToTop.addEventListener('click', () => {
        window.scrollTo({ top: 0, behavior: 'smooth' });
    });

    // 5. 代码复制功能
    function copyCode(button) {
        const codeBlock = button.closest('.code-block').querySelector('code');
        const text = codeBlock.textContent;

        navigator.clipboard.writeText(text).then(() => {
            const originalText = button.textContent;
            button.textContent = '已复制!';
            button.style.background = 'var(--success-color)';

            setTimeout(() => {
                button.textContent = originalText;
                button.style.background = 'var(--primary-color)';
            }, 2000);
        }).catch(err => {
            console.error('复制失败:', err);
            button.textContent = '复制失败';
            setTimeout(() => {
                button.textContent = '复制';
            }, 2000);
        });
    }
</script>
```

---

## 📝 内容组织规范 (Content Organization)

### 1. Section ID 命名规则

每个主要章节必须有唯一的 ID,用于目录导航:

```html
<section id="overview" class="section">...</section>
<section id="features" class="section">...</section>
<section id="installation" class="section">...</section>
<section id="usage" class="section">...</section>
<section id="configuration" class="section">...</section>
<section id="deployment" class="section">...</section>
<section id="troubleshooting" class="section">...</section>
```

**ID 命名规则:**
- 全部小写
- 单词用连字符 `-` 分隔
- 语义化,见名知意
- 与 TOC 链接一一对应

### 2. 标题层级规范

```html
<!-- h1: 仅用于页面主标题 (Hero Section) -->
<h1 class="hero-title">Xget - 超高性能开发者资源加速引擎</h1>

<!-- h2: 主要章节标题 -->
<h2 class="section-title">1. 快速开始</h2>

<!-- h3: 子章节标题 -->
<h3>安装步骤</h3>

<!-- h4: 更细分的小标题 -->
<h4>方法一: npm 安装</h4>
```

### 3. 常见内容模块

#### 特性展示 (Feature Grid)

```html
<div class="feature-grid">
    <div class="feature-item">
        <div class="icon">⚡</div>
        <h4>特性标题</h4>
        <p>特性描述</p>
    </div>
    <!-- 重复 2-6 个特性卡片 -->
</div>
```

#### 安装步骤 (Installation Steps)

```html
<div class="code-block">
    <div class="code-header">
        <span class="code-title">命令行安装</span>
        <button class="copy-btn" onclick="copyCode(this)">复制</button>
    </div>
    <pre><code><span class="comment"># 1. 安装依赖</span>
npm install package-name

<span class="comment"># 2. 运行项目</span>
npm start</code></pre>
</div>
```

#### 列表样式

```html
<ul class="list">
    <li><strong>项目标题</strong> - 项目描述</li>
    <li><strong>项目标题</strong> - 项目描述</li>
</ul>
```

```css
.list {
    list-style: none;
    padding-left: 0;
}

.list li {
    padding: 0.75rem 0;
    padding-left: 1.5rem;
    position: relative;
    color: var(--text-secondary);
    line-height: 1.8;
}

.list li::before {
    content: '▸';
    position: absolute;
    left: 0;
    color: var(--primary-color);
    font-weight: bold;
}

.list li strong {
    color: var(--text-color);
}
```

#### 提示框 (Info Box)

```html
<!-- 普通信息 -->
<div class="info-box">
    <strong>提示:</strong> 这是一个重要信息
</div>

<!-- 成功信息 -->
<div class="info-box success">
    <strong>成功!</strong> 操作已完成
</div>

<!-- 警告信息 -->
<div class="info-box warning">
    <strong>注意:</strong> 请谨慎操作
</div>
```

#### 平台标签 (Platform Tags)

```html
<div class="platform-tags">
    <span class="tag">GitHub</span>
    <span class="tag">npm</span>
    <span class="tag">Docker</span>
    <span class="tag">PyPI</span>
</div>
```

```css
.platform-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin: 1.5rem 0;
}

.tag {
    background: rgba(0, 102, 255, 0.1);
    color: var(--secondary-color);
    padding: 0.5rem 1rem;
    border-radius: 20px;
    border: 1px solid rgba(0, 212, 255, 0.3);
    font-size: 0.875rem;
    font-weight: 600;
    transition: all 0.3s ease;
}

.tag:hover {
    background: var(--primary-color);
    color: white;
    transform: translateY(-2px);
}
```

---

## ✅ 生成检查清单 (Quality Checklist)

生成 HTML 后,必须确认以下所有项目:

### 结构完整性
- [ ] DOCTYPE 和 html lang 属性正确
- [ ] 所有必需的 meta 标签已添加
- [ ] Hero Section 包含标题、副标题、元信息
- [ ] 主内容区域使用 flex 布局
- [ ] TOC Sidebar 在右侧,使用 sticky 定位
- [ ] 每个主要章节有唯一的 ID
- [ ] Footer 包含版权和备案信息占位
- [ ] 回到顶部按钮已添加

### 样式规范
- [ ] 所有 CSS 变量已定义
- [ ] 使用项目统一颜色方案
- [ ] 响应式媒体查询已添加 (1024px, 768px, 480px)
- [ ] 代码块有复制按钮
- [ ] 卡片和特性网格有 hover 效果
- [ ] 平滑滚动已启用

### JavaScript 功能
- [ ] config.json 加载逻辑已添加
- [ ] TOC 平滑滚动功能已实现
- [ ] TOC 激活状态跟踪 (IntersectionObserver) 已实现
- [ ] 回到顶部按钮显示/隐藏逻辑已添加
- [ ] 代码复制功能 `copyCode()` 已实现

### 内容质量
- [ ] 所有 section 标题与 TOC 一致
- [ ] 代码块有语法高亮注释 (comment class)
- [ ] 链接使用 `target="_blank"` 打开外部网站
- [ ] 图标使用 emoji 或 SVG
- [ ] 长列表使用自定义样式 `.list`

### 性能和可访问性
- [ ] 无外部字体加载 (使用系统字体栈)
- [ ] 图片使用相对路径
- [ ] 所有交互元素有 hover 效果
- [ ] 代码块内容可水平滚动

---

## 🚨 常见错误和禁止事项 (Common Mistakes)

### ❌ 绝对禁止

1. **不要加载外部字体**
   ```html
   <!-- ❌ 错误 -->
   <link href="https://fonts.googleapis.com/..." rel="stylesheet">
   ```

2. **不要使用外部 CSS 框架**
   ```html
   <!-- ❌ 错误 -->
   <link href="https://cdn.jsdelivr.net/npm/bootstrap..." rel="stylesheet">
   ```

3. **不要使用外部 JavaScript 库**
   ```html
   <!-- ❌ 错误 -->
   <script src="https://code.jquery.com/jquery..."></script>
   ```

4. **不要创建深色/浅色主题切换**
   - 项目固定使用深色主题

5. **不要使用复杂动画**
   - 仅使用简单的 `transition` 和 `transform`
   - 避免 `@keyframes` 动画

6. **不要忘记 TOC 必须在右侧**
   ```css
   /* ✅ 正确 */
   .toc-sidebar {
       order: 2; /* 在 flex 布局中排在右侧 */
   }
   ```

### ⚠️ 注意事项

1. **代码块语法高亮**: 使用 `<span class="comment">` 等手动标记
2. **响应式图片**: 在移动端可能需要调整大小
3. **长链接**: 在代码块中确保正确换行
4. **emoji 兼容性**: 优先使用 emoji,确保跨平台显示

---

## 📚 参考示例文件

以下是已完成的标准示例文件,严格遵循本规范:

1. **0101.xget-high-performance-resource.html** - 资源加速工具
2. **0102.bytebot-ai-desktop-agent.html** - AI 桌面代理
3. **0103.onyx-opensource-ai-platform.html** - AI 平台 (包含 Tab 切换)
4. **0104.trendradar-trend-monitoring-tool.html** - 趋势监控工具
5. **0105.tracy-profiler-performance-analysis.html** - 性能分析工具

**查看示例文件路径:**
```
/Volumes/Samsung/software_xare/quartz-online/public/chapter02/
```

---

## 🎯 AI 生成提示词模板

当你需要 AI 生成新的 HTML 文档时,使用以下提示词:

```
请根据 `/Volumes/Samsung/software_xare/quartz-online/docs/AI-HTML-Generation-Prompt.md`
中的硬性规范,生成一个关于 [项目名称] 的 HTML 技术文档。

项目信息:
- 项目名称: [名称]
- 项目描述: [简短描述]
- 许可证: [License]
- 主要语言: [Language]
- 平台: [Platform]
- GitHub: [URL]

内容要求:
1. 包含以下章节: [列出章节名称]
2. 展示 [X] 个核心特性
3. 提供安装步骤和使用示例
4. 包含配置说明和部署指南

严格遵循规范:
- 使用统一的 CSS 变量和样式
- TOC 必须在右侧 (order: 2)
- 包含所有必需的 JavaScript 功能
- 确保响应式设计完整
- 代码块必须有复制按钮
```

---

## 📞 支持和反馈

如有问题或建议,请查阅:
- **项目文档**: `/Volumes/Samsung/software_xare/quartz-online/README.md`
- **样式指南**: `/Volumes/Samsung/software_xare/quartz-online/docs/06.HTML书写风格.md`
- **示例文件**: `/Volumes/Samsung/software_xare/quartz-online/public/chapter02/`

---

**文档版本**: v1.0
**更新时间**: 2025-12-14
**适用项目**: Quartz Online - Halavah's Tech
**维护者**: 开发团队
