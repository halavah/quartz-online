# 腾讯云部署配置
# 用于本地构建并部署到腾讯云服务器
# 注意: 这是静态站点（Next.js static export），无需运行时环境变量，此文件仅用于构建过程

# 构建环境
NODE_ENV=production
BUILD_TARGET=tencent

# Cloudflare 认证（如果需要同时部署到 Cloudflare Pages）
# CLOUDFLARE_API_KEY=your_global_api_key_here
# CLOUDFLARE_EMAIL=your_email@example.com
# CLOUDFLARE_ACCOUNT_ID=your_account_id_here
