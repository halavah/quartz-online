# 02. 开发指南

## 📐 功能需求

### 核心功能模块

#### 1. 文章展示系统

**首页展示**：
- Hero 区域（站点标题 + 简介）
- 分类筛选按钮组（动态从文章提取）
- 视图模式切换（网格/列表）
- 文章卡片网格/列表布局
- 分页导航（每页 6 篇文章）

**文章详情页**：
- 导航栏
- 文章头部信息（标题、描述、分类）
- HTML 内容渲染区域
- 从 `public/` 目录加载外部 HTML 文件
- 如果文件不存在，显示友好提示
- 现代化功能：返回顶部、阅读进度、目录导航、深色模式、打印优化

#### 2. 搜索功能

**触发方式**：
- 点击导航栏搜索图标
- 弹出全屏搜索界面

**搜索特性**：
- **实时搜索**：支持搜索标题、描述、分类
- **毛玻璃效果**：半透明黑色背景 + 8px 模糊
- **渐变搜索框**：根据主题自动切换
- **结果展示**：最多显示 8 条结果，卡片式布局
- **快捷键**：ESC 关闭，自动聚焦输入框

**搜索算法**：
```typescript
// 简单的包含匹配
const query = searchQuery.toLowerCase();
return articles.filter(article =>
  article.title.toLowerCase().includes(query) ||
  article.description.toLowerCase().includes(query) ||
  article.category.toLowerCase().includes(query)
);
```

#### 3. 主题切换系统

**支持模式**：
- 暗色主题（默认）
- 明亮主题

**实现方式**：
- CSS 变量 + `data-theme` 属性
- localStorage 持久化保存
- 页面加载时自动恢复用户偏好
- 所有组件自动适配主题

**主题切换逻辑**：
```typescript
const toggleTheme = () => {
  const newTheme = theme === 'dark' ? 'light' : 'dark';
  setTheme(newTheme);
  document.documentElement.setAttribute('data-theme', newTheme);
  localStorage.setItem('theme', newTheme);
};
```

#### 4. 分类筛选系统

**功能特性**：
- 自动从 articles.json 提取所有唯一分类
- 动态生成分类按钮
- 显示每个分类的文章数量
- 点击分类按钮筛选对应文章
- "全部"按钮显示所有文章
- 筛选后自动重置分页到第一页

**分类系统**：
- 支持任意自定义分类
- 分类自动按字母顺序排序
- 无需在代码中硬编码分类

#### 5. 视图模式切换

**视图类型**：
- **网格视图**（默认）：3列卡片布局，信息展示丰富
- **列表视图**：单列布局，适合快速浏览

**功能特性**：
- 视图状态自动保存到 localStorage
- 切换时保持当前分类和分页
- 响应式设计，移动端自动调整为单列

#### 6. 分页系统

**分页设置**：
- 每页显示 6 篇文章
- 可在 `app/page.tsx` 中修改 `ARTICLES_PER_PAGE` 常量

**交互体验**：
- 点击页码跳转
- 自动滚动到页面顶部
- 显示当前页码高亮
- 只显示实际需要的页码（省略号处理）

#### 7. 广告横幅系统

**功能特性**：
- 导航栏右侧显示广告横幅
- 支持关闭（关闭后整个会话不再显示）
- 点击横幅跳转到广告链接
- 关闭按钮不触发链接点击
- 渐变背景，视觉突出

---

## 🎨 设计规范

### 1. 颜色系统

#### 暗色主题（Dark Theme）

```css
:root[data-theme="dark"] {
  /* 背景色 */
  --dark-bg: #0a0e27;              /* 深色背景 */
  --card-bg: #1a1f3a;              /* 卡片背景 */

  /* 主色调 */
  --primary-color: #0066ff;         /* 主色 - 蓝色 */
  --gradient-primary: linear-gradient(135deg, #0066ff 0%, #00d4ff 100%);

  /* 文字颜色 */
  --text-color: #e2e8f0;           /* 主要文字 */
  --text-secondary: #94a3b8;       /* 次要文字 */

  /* 边框 */
  --border-color: rgba(148, 163, 184, 0.1);
}
```

#### 明亮主题（Light Theme）

```css
:root[data-theme="light"] {
  /* 背景色 */
  --dark-bg: #ffffff;              /* 白色背景 */
  --card-bg: #f8f9fa;              /* 浅灰卡片背景 */

  /* 主色调 */
  --primary-color: #0066ff;         /* 主色 - 蓝色 */
  --gradient-primary: linear-gradient(135deg, #0066ff 0%, #00d4ff 100%);

  /* 文字颜色 */
  --text-color: #1a202c;           /* 深色文字 */
  --text-secondary: #64748b;       /* 灰色文字 */

  /* 边框 */
  --border-color: rgba(100, 116, 139, 0.2);
}
```

#### 功能色

```css
/* 成功 */
--success-color: #10b981;

/* 警告 */
--warning-color: #f59e0b;

/* 错误 */
--error-color: #ef4444;

/* 信息 */
--info-color: #3b82f6;
```

### 2. 布局规范

#### 容器宽度

```css
/* 最大内容宽度 */
.max-w-7xl {
  max-width: 80rem;  /* 1280px */
}

/* 文章内容宽度 */
.prose {
  max-width: 65ch;
}
```

#### 间距系统

使用 Tailwind CSS 的间距 scale：

| 类名 | 值 | 用途 |
|------|-----|------|
| `p-4` | 1rem (16px) | 小型内边距 |
| `p-6` | 1.5rem (24px) | 中型内边距 |
| `py-12` | 3rem (48px) | 区块垂直间距 |
| `py-20` | 5rem (80px) | 大区块间距 |
| `gap-6` | 1.5rem (24px) | 网格/Flex 间距 |

#### 网格布局

```css
/* 文章卡片网格 */
grid-cols-1 md:grid-cols-2 lg:grid-cols-3

/* 响应式断点 */
- 手机：1 列
- 平板（md: 768px+）：2 列
- 桌面（lg: 1024px+）：3 列
```

### 3. 字体排版

#### 字体家族

```css
body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI',
               'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei',
               sans-serif;
}
```

#### 字体大小

| 元素 | 类名 | 大小 | 用途 |
|------|------|------|------|
| 标题 | `text-5xl` | 3rem (48px) | Hero 标题 |
| 副标题 | `text-3xl` | 1.875rem (30px) | 区块标题 |
| 文章标题 | `text-xl` | 1.25rem (20px) | 卡片标题 |
| 正文 | `text-base` | 1rem (16px) | 正文内容 |
| 小字 | `text-sm` | 0.875rem (14px) | 元信息 |

#### 行高

```css
/* 标题 */
line-height: 1.2;  /* 紧凑 */

/* 正文 */
line-height: 1.6;  /* 舒适阅读 */
```

### 4. 组件设计规范

#### 文章卡片（Article Card）

**尺寸**：
- 宽度：自适应网格
- 内边距：2rem (32px)
- 圆角：1rem (16px)

**状态样式**：
```css
/* 默认 */
background: var(--card-bg);
border: 1px solid var(--border-color);

/* Hover */
transform: scale(1.05);
box-shadow: 0 20px 40px rgba(0, 102, 255, 0.2);
border-color: var(--primary-color);
```

**内部结构**：
```
┌────────────────────────────┐
│ [分类标签]          [#01]  │  ← 顶部元信息
│                            │
│ 文章标题                   │  ← 标题（20px，粗体）
│                            │
│ 文章描述内容...            │  ← 描述（16px，次要色）
│                            │
│ 阅读全文 →                 │  ← 底部操作
└────────────────────────────┘
```

#### 导航栏（Navbar）

**高度**：自适应内容，垂直内边距 1rem

**布局**：
```
┌──────────────────────────────────────────────────────────────────────────┐
│ [Logo]        [搜索按钮][主题切换]  |  [广告横幅][关闭按钮] | [菜单]    │
└──────────────────────────────────────────────────────────────────────────┘
```

**样式**：
```css
/* 背景 */
backdrop-filter: blur(12px);
background: rgba(10, 14, 39, 0.9);  /* 暗色主题 */

/* 粘性定位 */
position: sticky;
top: 0;
z-index: 50;
```

**广告横幅**：
```css
/* 渐变背景 */
background: linear-gradient(135deg, #ff6b00 0%, #ff9500 100%);
color: white;
/* 关闭按钮 */
background: rgba(255, 255, 255, 0.2);
backdrop-filter: blur(4px);
```

#### 搜索弹窗（Search Modal）

**布局**：
- 全屏遮罩
- 居中显示搜索框
- 最大宽度 48rem (768px)

**设计特性**：
```css
/* 遮罩 */
background: rgba(0, 0, 0, 0.8);
backdrop-filter: blur(8px);

/* 搜索框 */
background: linear-gradient(135deg,
  rgba(15, 23, 42, 0.95) 0%,
  rgba(30, 41, 59, 0.95) 100%);
border: 1px solid var(--border-color);
border-radius: 1rem;
backdrop-filter: blur(20px);
```

**搜索结果卡片**：
```css
/* 默认 */
background: rgba(51, 65, 85, 0.3);
border: 1px solid transparent;

/* Hover */
background: rgba(59, 130, 246, 0.1);
border-color: #3b82f6;
transform: scale(1.02);
```

#### 视图切换按钮

**布局**：
- 右上角，分类统计旁边
- 水平排列，左侧圆角，右侧圆角

**按钮样式**：
```css
/* 容器 */
border: 1px solid var(--border-color);
border-radius: 0.5rem;

/* 激活状态 */
background: #3b82f6;
color: white;

/* 非激活状态 */
background: transparent;
color: var(--text-secondary);
```

#### 分页器（Pagination）

**布局**：
- 水平居中
- 按钮间距 0.5rem

**按钮样式**：
```css
/* 默认 */
width: 2.5rem;
height: 2.5rem;
border-radius: 0.5rem;
background: var(--card-bg);

/* 当前页 */
background: var(--gradient-primary);
color: white;
box-shadow: 0 5px 15px rgba(0, 102, 255, 0.3);

/* Hover */
border-color: var(--primary-color);
```

#### 文章列表视图

**布局**：
- 单列排列
- 每个条目包含：分类标签、序号、标题、描述、箭头图标
- padding: 1.5rem
- hover 效果：轻微缩放、阴影

**样式**：
```css
/* 条目容器 */
background: var(--card-bg);
border: 1px solid var(--border-color);
border-radius: 0.5rem;
transition: all 0.3s ease;

/* hover */
transform: scale(1.02);
box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
```

### 5. 动画与过渡

#### 通用过渡

```css
/* 所有交互元素 */
transition: all 0.3s ease;
```

#### 卡片悬停

```css
.card:hover {
  transform: scale(1.05);
  transition: transform 0.3s ease;
}
```

#### 搜索弹窗动画

```css
/* 遮罩淡入 */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

/* 搜索框下滑 */
@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

---

## ⚙️ 配置参考

### 1. Next.js 配置（next.config.ts）

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',           // 启用静态导出
  images: {
    unoptimized: true         // 静态导出时禁用图片优化
  },
  trailingSlash: true,        // URL 末尾添加斜杠
};

export default nextConfig;
```

**配置说明**：

| 配置项 | 值 | 说明 |
|--------|-----|------|
| `output` | `'export'` | 构建时生成纯静态 HTML 文件 |
| `images.unoptimized` | `true` | 禁用 Next.js 图片优化，适配静态导出 |
| `trailingSlash` | `true` | URL 末尾添加 `/`，提升静态服务器兼容性 |

### 2. TypeScript 配置（tsconfig.json）

```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

### 3. Tailwind CSS 配置（tailwind.config.ts）

```typescript
import type { Config } from "tailwindcss";

export default {
  content: [
    "./pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        background: "var(--background)",
        foreground: "var(--foreground)",
      },
    },
  },
  plugins: [],
} satisfies Config;
```

### 4. 文章配置（data/articles.json）

#### 配置结构

```json
{
  "config": {
    "siteName": "Halavah's Tech",
    "siteDescription": "探索前沿技术，提升开发效率",
    "githubUrl": "https://github.com/halavah",
    "beianNumber": "京ICP备12345678号",
    "copyrightText": "© 2025 Halavah's Tech. All rights reserved.",
    "adLink": "https://api.nekoapi.com"
  },
  "articles": [
    {
      "title": "文章标题",
      "description": "文章简介描述",
      "htmlFile": "chapter01/01.article.html",
      "category": "前端框架"
    }
  ]
}
```

#### 字段说明

**站点配置（config）**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `siteName` | string | ✅ | 站点名称，显示在导航栏和页面标题 |
| `siteDescription` | string | ✅ | 站点描述，显示在 Hero 区域 |
| `githubUrl` | string | ❌ | GitHub 链接（可选） |
| `beianNumber` | string | ❌ | 备案号，显示在页脚 |
| `copyrightText` | string | ❌ | 版权信息，显示在页脚 |
| `adLink` | string | ❌ | 广告链接，用于导航栏横幅 |

**文章配置（articles）**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `title` | string | ✅ | 文章标题 |
| `description` | string | ✅ | 文章简介，显示在列表卡片 |
| `htmlFile` | string | ✅ | HTML 文件路径（相对于 `public/`） |
| `category` | string | ✅ | 文章分类（任意自定义分类） |

#### 配置示例

```json
{
  "config": {
    "siteName": "Halavah's Tech",
    "siteDescription": "探索前沿技术，提升开发效率",
    "githubUrl": "https://github.com/halavah"
  },
  "articles": [
    {
      "title": "深入理解 React Server Components",
      "description": "探索 React 18 引入的服务端组件，了解其工作原理和最佳实践。",
      "htmlFile": "chapter01/01.react-server-components.html",
      "category": "前端框架"
    },
    {
      "title": "AI 编程助手对比评测",
      "description": "对比 GitHub Copilot、Cursor、Claude Code 等主流 AI 编程工具。",
      "htmlFile": "chapter02/01.ai-coding-tools.html",
      "category": "AI工具"
    }
  ]
}
```

### 5. 全局样式（app/globals.css）

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* ============================================
   主题系统 - CSS 变量
   ============================================ */

/* 暗色主题（默认） */
:root[data-theme="dark"] {
  --dark-bg: #0a0e27;
  --card-bg: #1a1f3a;
  --primary-color: #0066ff;
  --gradient-primary: linear-gradient(135deg, #0066ff 0%, #00d4ff 100%);
  --text-color: #e2e8f0;
  --text-secondary: #94a3b8;
  --border-color: rgba(148, 163, 184, 0.1);
}

/* 明亮主题 */
:root[data-theme="light"] {
  --dark-bg: #ffffff;
  --card-bg: #f8f9fa;
  --primary-color: #0066ff;
  --gradient-primary: linear-gradient(135deg, #0066ff 0%, #00d4ff 100%);
  --text-color: #1a202c;
  --text-secondary: #64748b;
  --border-color: rgba(100, 116, 139, 0.2);
}

/* ============================================
   全局样式
   ============================================ */

body {
  color: var(--text-color);
  background: var(--dark-bg);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI',
               'PingFang SC', 'Hiragino Sans GB', sans-serif;
  transition: background-color 0.3s ease, color 0.3s ease;
}

/* 滚动条样式 */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: var(--dark-bg);
}

::-webkit-scrollbar-thumb {
  background: var(--border-color);
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--text-secondary);
}
```

### 6. 包管理配置（package.json）

```json
{
  "name": "quartz-online",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "15.1.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^9",
    "eslint-config-next": "15.1.5",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
```

---

## 🔧 开发工作流

### 1. 启动开发服务器

```bash
npm run dev
```

访问：http://localhost:3000

### 2. 修改配置后的步骤

**修改 `articles.json` 后**：
- 保存文件
- 刷新浏览器即可看到变化

**修改 CSS 变量后**：
- 保存 `globals.css`
- ���重载自动生效

**修改组件后**：
- 保存 `.tsx` 文件
- 热重载自动生效

### 3. 类型检查

```bash
# 运行 TypeScript 类型检查
npx tsc --noEmit
```

### 4. 代码格式化

建议使用 Prettier 配置：

```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5"
}
```

---

## 📝 开发注意事项

### 1. 主题适配

所有新增组件必须使用 CSS 变量，确保主题切换正常：

```css
/* ✅ 正确 */
background: var(--card-bg);
color: var(--text-color);

/* ❌ 错误 */
background: #1a1f3a;
color: #e2e8f0;
```

### 2. 响应式设计

所有布局必须考虑移动端适配：

```tsx
// 使用 Tailwind 响应式类
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
```

### 3. 性能优化

- 使用 `useMemo` 缓存计算结果
- 使用 `useCallback` 缓存函数引用
- 避免不必要的重渲染

```typescript
// 使用 useMemo 优化搜索性能
const searchResults = useMemo(() => {
  if (!searchQuery.trim()) return [];
  return articles.filter(/* ... */);
}, [searchQuery, articles]);
```

### 4. 静态导出限制

**不支持的特性**：
- 服务端 API 路由（`app/api/`）
- 动态路由的 `getServerSideProps`
- Image Optimization（需设置 `unoptimized: true`）
- Internationalized Routing

**支持的特性**：
- 静态路由
- 客户端数据获取
- CSS 模块
- Tailwind CSS

---

## 🎯 最佳实践

### 1. 组件设计

- **单一职责**：每个组件只做一件事
- **可复用性**：通过 props 提供灵活性
- **类型安全**：使用 TypeScript 定义 Props 类型

### 2. 状态管理

- **局部状态**：使用 `useState`
- **派生状态**：使用 `useMemo`
- **副作用**：使用 `useEffect`

### 3. 代码组织

```
app/
├── components/        # 可复用组件
│   ├── Navbar.tsx
│   └── Pagination.tsx
├── page.tsx          # 页面组件
└── globals.css       # 全局样式
```

### 4. 命名规范

- **组件**：PascalCase（`ArticleCard.tsx`）
- **函数**：camelCase（`handleClick`）
- **CSS 变量**：kebab-case（`--primary-color`）
- **文件名**：与导出组件同名

---

祝您开发愉快！如有问题，请参考 [内容管理](./03.内容管理.md) 或 [部署与运维](./04.部署与运维.md)。
